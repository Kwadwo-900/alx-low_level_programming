#include <stdio.h>
/**
 * main - prints all single digits numbers base 10
 * Return: prints 0 when successful
 */

int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
