#include "main.h"

/**
 * _abs - finds the absolute value of an integer
 * @n: the int value to be checked
 * Return: the absolute of the value int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
