#include "main.h"

/**
 * mul - take two integers and multiply
 * @a: integer to be multiplied to b
 * @b: integer to be multiplied to a
 * Return: 1 on success, 0 otherwise
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
